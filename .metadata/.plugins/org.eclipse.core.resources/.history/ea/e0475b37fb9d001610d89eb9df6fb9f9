package Chap7;

import java.util.HashSet;
import java.util.Set;

public class Jogador {
	private Set<Carta> mao = new HashSet<>();
	
	public void addCarta(Carta carta){
		mao.add(carta);
	}
	
	public Carta play(Carta carta){
		Carta car = mao.get(tipo);
		mao.remove(tipo);
		
		return car;
	}
}
